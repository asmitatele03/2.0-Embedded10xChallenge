📟 LCD Scrolling Text Program – Embedded C (ARM LPC21xx)

This program demonstrates scrolling text animation on a 16×2 LCD using Embedded C on the LPC21xx ARM7 microcontroller.
The code takes a predefined string (`"vector"`) and shifts it across the LCD screen by repeatedly moving the cursor and re-printing the string.

🧠 Explanation of the Code

1. Header Files

```c
#include <lpc21xx.h>
#include "header.h"
```

* `lpc21xx.h` → Contains register definitions for the LPC2129/LPC2148 ARM7 microcontroller.
* `header.h` → Your custom header file with LCD function prototypes (`lcd_init`, `lcd_cmd`, `lcd_string`, etc.).

---

### **2. Macro Definition**

```c
#define S "vector"
```

* Defines the string to be scrolled on the LCD.
* You can replace `"vector"` with any word you want to scroll.

---

### **3. Main Function**

```c
int main()
{
    int i, j, k;

    lcd_init();         // Initializes LCD in 4-bit/8-bit mode
    lcd_cmd(0x01);      // Clears the display
```

* Variables:

  * `i` → loop counter
  * `j` → stores string length
  * `k` → stores the shifting index

---

### **4. Main Loop**

```c
while(1)
{
    j = strlen(S);  // Find length of the string
    k = 1;          // Start shifting from index 1
```

* `strlen()` determines how many characters are in `"vector"`.

---

### **5. Scrolling Logic**

```c
for(i = 0; i < 16; i++)
{
    lcd_cmd(0x80 + i);   // Move cursor to the next LCD column
    lcd_string(S);        // Print the full string

    delay_ms(500);        // Delay for visibility
    lcd_cmd(0x01);        // Clear display

    k++;
    if(k == j)
        k = 0;

    lcd_string(S + k);    // Print string starting from index 'k'
}
```

### 🔍 **How the Scrolling Works**

* `0x80 + i` → Moves the cursor across the 1st row of the LCD.
* `lcd_string(S)` → Prints `"vector"` normally.
* `lcd_cmd(0x01)` → Clears the screen to show the next shifted version.
* `lcd_string(S + k)` → Prints the string starting from index `k`.

#### Example:

If `S = "vector"`

| k | Printed String      |
| - | ------------------- |
| 0 | vector              |
| 1 | ector               |
| 2 | ctor                |
| 3 | tor                 |
| 4 | or                  |
| 5 | r                   |
| 6 | (loops back) vector |

This creates a **dynamic scrolling animation** on the LCD.

---

## 📝 **Features**

* Works with **LPC21xx ARM7** microcontrollers
* LCD driver fully in C
* Demonstrates:

  * Cursor shifting
  * String manipulation
  * LCD command control
  * Dynamic scrolling effect

---

## 🧪 **Hardware Requirements**

* LPC2129 / LPC2148 ARM7 board
* 16×2 LCD (HD44780-based)
* Potentiometer for contrast
* Jumper wires
* Power supply

---

If you want, I can also create:
📌 A **GitHub-ready README.md file**
📌 A **repository name + emoji**
📌 A **diagram for LCD connections**
📌 A **YouTube-style description**

Just tell me!

